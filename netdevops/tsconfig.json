{
  "files": [], // 空数组，表示这个配置文件本身不直接包含任何源文件

  "references": [ // 项目引用配置，用于分割 TypeScript 项目
    {
      "path": "./tsconfig.node.json" // 引用 Node.js 环境的 TypeScript 配置
                                    // 通常用于构建工具和配置文件的类型检查
    },
    {
      "path": "./tsconfig.app.json" // 引用应用程序的 TypeScript 配置
                                   // 用于实际的 Vue 应用源代码的类型检查
    }
  ]
}
