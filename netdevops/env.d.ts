/// <reference types="vite/client" /> // 这是一个三斜线指令，用于在 TypeScript 中引入类型声明文件
                                     // 具体作用：
                                     // 1. 告诉 TypeScript 编译器引入 Vite 客户端的类型定义
                                     // 2. 使得在项目中可以使用 Vite 提供的类型定义，如 import.meta.env 等
                                     // 3. 提供更好的类型检查和代码提示功能
                                     // 4. 这个声明通常放在项目的类型声明文件中（如 env.d.ts）
